# Browser — public by design (anon key). Persistence is Phase 4.
# Never put service_role, AUTH_SECRET, or DATABASE_URL here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Tools / CI only — USDA FoodData Central write-time nutrition (Phase 6).
# Do not prefix with NEXT_PUBLIC_. Do not import this in src/.
USDA_FDC_API_KEY=
